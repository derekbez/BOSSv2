{
  "id": "random_local_place_name",
  "title": "Random Local Place Name",
  "description": "Displays a randomly selected place name from a local dataset or API.",
  "entry_point": "main.py",
  "config": {
    "data_source": "assets/places.json (local) or external API",
    "display_format": "Place name as text"
  },
  "assets_required": [
    "places.json (list of place names, one per line or as a JSON array)"
  ],
  "api_keys": [],
  "instructions": "Add a places.json file to the assets/ folder with a list of place names. Optionally, configure an API endpoint in config if using an online source."
}
