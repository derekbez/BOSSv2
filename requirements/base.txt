# Runtime dependencies for B.O.S.S. (install on Raspberry Pi and production environments)

gpiozero
lgpio
# TM1637 7-seg display (GPIO) — Linux only
python-tm1637; platform_system == "Linux"

# Screen backend (textual only; rich is a transitive dependency)
textual>=0.58

# HTTP client for external API mini-apps
requests>=2.32,<3
